extends layout

block content
  h1= title
  h4
    a(href="/") Home

  ul
    each evento in eventos
        li
            strong #{ evento.eventos }
            p ID: #{ evento.id }
            p Tipo de Evento: #{evento.tipo}
            p Fecha: #{evento.fecha}
            p Materia: #{evento.materia}